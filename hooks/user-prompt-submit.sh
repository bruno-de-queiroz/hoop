#!/usr/bin/env bash
# UserPromptSubmit hook: check pending admission requests.
# Calls hoop_check_admissions via the MCP server so the host
# can admit or deny peers waiting to join the session.
#
# TODO: Wire to MCP tool call in a future ticket.
exit 0
